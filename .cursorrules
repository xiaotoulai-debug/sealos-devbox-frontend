# EMAG 跨境电商管理系统 - 全局开发规范与 AI 协作指令

## 0. 🤖 AI 强制接头暗号 (CRITICAL)
- **身份确认与唤醒**: 每次开始新的对话、或者接收到新的开发任务时，**你必须在回复的第一句话尊称用户为“老板”**。这代表你已经完整加载并理解了本 `.cursorrules` 中的所有架构铁律。如果你没有叫“老板”，说明你失忆了，用户有权拒绝你的任何代码方案。

## 1. 项目定位与技术栈
- **核心目标**: 本项目是一个面向跨境电商（主营 eMAG 平台）的高效选品、利润核算与团队协同系统，支持多国多店（如 RO/BG/HU）数据同源管理。
- **前端 (Frontend)**: React 18 + TypeScript + Vite + Ant Design + Tailwind CSS
- **后端 (Backend)**: Node.js + Express + TypeScript
- **数据库 (Database)**: PostgreSQL + Prisma ORM

## 2. eMAG 业务架构专属铁律 (Global Architecture Rules)
- **绝对禁止硬编码特判 (No Hardcoding)**: 绝不允许出现 `if (region === 'BG')` 或特定 `shopId` 的特判。时区、货币（BG=EUR, RO=RON, HU=HUF）、API 域名、国家图标必须通过配置字典或数据库动态读取。
- **单一数据管线 (Single Source of Truth Pipeline)**: 不论同步哪个站点的产品或订单，必须经过唯一的 `Normalizer`（数据清洗器）。修复 Bug 或兼容新数据结构，只能在这个统一管线内修改。
- **两段式深度抓取 (Two-Stage Deep Sync)**: eMAG 产品同步必须分两步：
  1. 调用 Offer API 快速拉取 SKU、价格、库存等基础业务数据。
  2. 提取无图 SKU，批量调用 Catalog API (如 product/read) 深层抓取图片。
- **黄金图片提纯算法**: 在解析 eMAG 图片时，必须过滤跳过任何包含 `logo`、`emag-placeholder` 或 `.svg` 的无效链接，确保入库的 `main_image` 绝对纯净。

## 3. 动态业务权限体系 (Dynamic RBAC)
- **子账号与登录系统**: 必须包含独立的 `/login` 前端登录页，严禁开放外部注册。所有子账号由具有“账号管理”权限的员工在后台统一集中派发。
- **树状权限控制**: 严禁硬编码 `ADMIN/STAFF`。必须采用 `User -> Role -> Permission` 架构，支持自定义角色。
- **精细化与数据隔离**: 权限必须管控到菜单级、按钮级。涉及业务流转的数据（如入选产品、订单），查询时必须联合当前用户的 `userId` 和角色数据权限进行 SQL/Prisma 级别安全过滤。

## 4. 防御性编程规范 (严防坠毁与白屏)
- **后端并发与防死锁**: 同步引擎处理多店数据时必须有隔离性；所有 API 调用必须有完整的 `try...catch...finally` 结构，无论发生何种崩溃，`finally` 中必须释放并发同步锁。
- **前端数据安全渲染**: 执行 `.map()`, `.forEach()` 等数组方法前，必须使用 `Array.isArray(data)` 校验并配合可选链 (`?.`)。若数据异常，返回 `<Empty />`，绝不允许页面白屏。
- **前端哑巴渲染与优雅兜底**:
  - 前端禁止自行拼接货币符号或跳转链接，100% 依赖后端返回字段。
  - 图片列必须使用支持 `fallback/onError` 的高级 `<Image>` 组件，遇到空图或 404 显示默认占位图，严禁出现浏览器原生碎图图标。
- **统一响应格式**: 成功返回 `{ code: 200, data: [.../object], message: 'success' }`；失败返回 `{ code: 500/400, data: null, message: '真实后端错误信息' }`。列表为空时 `data` 必须是 `[]`。

## 5. UI/UX 与全局弹窗设计规范 (Working Modal)
- **视觉标准**: 遵循 Google Material Design 极简风格，留白充足。财务强相关数据列对齐，盈亏状态使用红绿警示色。顶部导航右侧展示带有角色标签的 UserInfo。
- **标准弹窗结构**:
  - **头部**: 包含“图标 + 标题 + 数量 Tag”，下方固定展示灰色操作引导语。
  - **主体**: 采用列表/表格结构，表头深色背景。每行固定展示图片、SKU 详情、操作表单。列表区域需设置 `maxHeight` 与 `overflowY: 'auto'`。
  - **底部**: 包含统计栏（红色加粗大字），按钮右对齐（取消在前，确认在后），危险按钮靠左放置。

### 🎨 Table & Layout Standards（表格与布局规范）

1. **全局吸顶效果（Sticky Header）**
   - 所有页面级 `<Table>` 或 `<ProTable>` 必须启用**内部纵向滚动**，以固定表头、避免依赖外层 `window` / Layout 滚动导致 `sticky` 失效。
   - **必须**配置：`scroll={{ y: 'calc(100vh - Xpx)', x: 'max-content' }}`。
   - **`Xpx` 计算规则**：按当前页「顶部导航栏 + PageHeader/标题区 + Toolbar/SearchForm + 分页器预留 + 页面内边距」等占用高度**估算**，通常落在 **250px～320px**；以实际页面为准微调，保证表格可视区高度合理、不出现双滚动条冲突。

2. **禁止页面级滚动（与上条配合）**
   - 配合 Table 的内部滚动，列表页外层容器应尽量使用 `height: 100vh` 或 `height: 100%`，并设置 `overflow: hidden`（或等价约束），**确保仅表格主体数据区域出现纵向滚动条**，避免整页与表格同时滚动、表头“吸顶”感知丢失。

3. **实现注意**
   - 父级容器慎用 `overflow: hidden` **直接包住**需 `position: sticky` 的非 Table 元素；Table 使用 `scroll.y` 时以 Ant Design 表格内部滚动为准。
   - 长列表页面新增或改版时，优先按本条检查 `scroll` 与外层布局，保持全站一致体验。

## 6. 工作流契约 (Workflow Contract)
- **强隔离与基建铁律**: 开发端口前端 5173，后端 3001。数据库表结构修改必须且只能通过修改 `prisma/schema.prisma` 并运行 `npx prisma migrate dev`，严禁直接写 SQL。
- **不删代码**: 除非明确指令，不允许静默删除与当前任务无关的配置、注释或业务代码。
- **架构文档驱动开发 (CRITICAL)**:
  - 每次开发新功能前，必须先静默读取 `docs/ARCHITECTURE.md` 了解系统现状，绝不允许写出破坏现有流水线的代码。
  - 每次完成重大模块后，代码跑通时，必须主动询问用户：“需不需要为您更新 `docs/ARCHITECTURE.md` 架构文档？”

## 7. 数据渲染规范
- 前端严禁直接调用任何第三方平台（如 eMAG, 1688）的 API。
- 渲染表格或表单时，严格依赖我们的自建后端 API 契约。
- 遇到图片渲染，统一读取我们自己接口返回的 `imageUrl` 或 `mainImage` 字段。

## 8. AI 执行反馈强制规则

每次接到任何前端开发、排查、修复、Build、Git、部署相关任务时，必须遵守以下规则：

### 1. 执行前必须阅读

任务开始前，必须先阅读本仓库根目录的：

- .cursorrules
- 改进优化内容.md（如果存在）
- 前端反馈.md（如果存在）

### 2. 执行过程中必须记录

任务执行过程中，必须记录以下信息，方便最终汇报：

- 本次任务目标
- 实际修改了哪些文件
- 每个文件为什么要改
- 是否存在不确定点
- 是否存在扩大修改范围
- 是否运行 npm run build
- 是否运行 npm run lint
- 是否运行 typecheck
- 是否执行 git commit
- 是否执行 git push
- 是否存在未提交文件
- 是否存在未解决问题

### 3. 任务结束后必须覆盖更新 前端反馈.md

每次任务结束，无论成功、失败、部分完成、被阻塞，都必须更新根目录：

- 前端反馈.md

更新方式必须是“完整覆盖写入”，不是追加。

也就是说：

- 前端反馈.md 永远只保留最近一次任务的最终汇报
- 不保留历史记录
- 不追加旧内容
- 不把上一轮任务内容留在文件里

### 4. 前端反馈.md 的内容来源

你在 Cursor 右侧 Agent 对话框最后准备发给老板的最终汇报内容，必须同步完整写入 前端反馈.md。

要求：

- 聊天里最终汇报什么，前端反馈.md 就写什么
- 文件内容必须完整，不能只写“已完成”
- 文件内容必须能让老板直接拖给 GPT/Gemini 分析
- 文件中必须说明过程、结果、验证、Git 状态、风险

### 5. 前端反馈.md 固定结构

每次覆盖写入 前端反馈.md 时，必须使用以下结构：

# 前端反馈

## 1. 本次任务目标

说明老板本次要求做什么。

## 2. 实际改动文件

逐项列出文件路径，并说明每个文件改了什么。

示例：

- `src/pages/xxx.tsx`：调整了什么逻辑/样式/交互
- `src/components/xxx.tsx`：新增或修改了什么组件

如果没有修改文件，也必须明确写：

- 本次未修改业务文件

## 3. 执行过程

说明关键执行步骤，不能只写“已完成”。

必须包括：

- 先检查了哪些文件
- 如何判断问题
- 如何修改
- 是否遇到报错
- 如何处理报错

## 4. 验证结果

必须写清楚：

- `npm run build` 是否执行，是否通过
- `npm run lint` 是否执行，是否通过
- `typecheck` 是否执行，是否通过
- 如果 package.json 中没有对应脚本，必须写明“package.json 中无该脚本，未执行”
- 如果存在 warning，必须列出 warning 内容和影响判断

## 5. Git 状态

必须写清楚：

- 当前分支名
- 是否 commit
- commit hash
- commit message
- 是否 push
- 远端分支是否同步
- working tree 是否 clean
- 是否存在未跟踪文件
- 是否存在不应该提交的文件

## 6. 风险与遗留问题

必须写清楚：

- 是否有未解决问题
- 是否有需要老板确认的点
- 是否有 warning
- 是否需要后端配合
- 是否存在可能影响线上功能的风险

如果没有风险，也必须明确写：

- 暂未发现阻塞风险

## 7. 给老板的最终结论

用 3 到 5 句话直接说明：

- 本次是否完成
- build 是否通过
- git 是否已提交/推送
- 是否可以进入下一步
- 是否需要老板确认

### 6. 文件处理要求

1. 如果 前端反馈.md 不存在，请创建。
2. 如果 前端反馈.md 已存在，后续每次任务完成后必须全量覆盖。
3. 不要在 前端反馈.md 里保留历史记录。
4. 不要追加写入。
5. 除非老板明确要求，否则不要把 前端反馈.md 提交到 GitHub。
6. 如果 .gitignore 中没有 前端反馈.md，请加入。
